<h1>
  <%= @task.name %>

  <% if @task.completed %>
    <%= "[X]" %>
  <% else %>
    <%= "[ ]" %>
  <% end %>

</h1>

<h2>
  Task Completed? <br>
  <%= @task.completed %>
</h2>

<%= link_to "Back to Home", tasks_path %>
